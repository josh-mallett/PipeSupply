import React from 'react';
import ReactDOM from 'react-dom';
import Main from './Components/main';
import '../../../content/styles/main.sass';

let container = document.getElementById('app');

ReactDOM.render(
  <Main />, container
);